apiVersion: v1
kind: Service
metadata:
  name: books-api-service
  namespace: production
  labels:
    app: books-api
spec:
  selector:
    app: books-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP